#To access and utilize this workflow, go to CODE, download as zip file, extract the .ipynb file and run through Anaconda for use in jupyterhub

#For viewing, just click on the file and a preview of the workflow will generate. The interactive features with ipywidgets generated outputs and cannot be viewed in this format
